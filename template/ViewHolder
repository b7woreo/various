#if (${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import android.support.v7.widget.RecyclerView;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

#parse("File Header.java")
public class ${NAME} extends RecyclerView.ViewHolder {

  public static ${NAME} create(LayoutInflater inflater, ViewGroup container) {
    View itemView = inflater.inflate(R.layout.${LAYOUT_ID}, container, false);
    return new ${NAME}(itemView);
  }

  private ${NAME}(View itemView) {
    super(itemView);
  }

  public void bind(${ITEM_CLASS} item) {
    
  }
}